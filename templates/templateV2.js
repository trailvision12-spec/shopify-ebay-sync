module.exports = function templateV2(product) {
    return `
        <!-- TEMPLATE VERSION 2 (WORK IN PROGRESS) -->
        <h2>${product.title}</h2>

        <p>This is the upcoming Blueseal template (v2).</p>

        <!-- Add Why Choose Blueseal improvements here -->
    `;
};
